/** Application-wide constants shared across API modules. */

// Resume Upload - Accepted file types and size limit
export const RESUME_ALLOWED_EXTENSIONS = ["pdf", "docx"];
export const RESUME_MAX_BYTES = 5 * 1024 * 1024;
