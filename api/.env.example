# Supabase - Copy to .env and fill in from Project Settings > API Keys
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Auth.js - Public URL of the auth routes as the browser sees them (through the
# frontend's /api proxy), so sign-in redirects return to the frontend.
AUTH_URL=http://localhost:3000/api/auth
